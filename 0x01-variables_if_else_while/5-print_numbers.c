#include <stdio.h>

/**
 * main - Entry point
 * Description: Prints digits from 0 to 9
 * Return: Always 0 (Succes).
 */
int main(void)
{
	char ch[] = "0123456789";

	printf("%s\n", ch);
return (0);
}
