#include "main.h"

/**
 * add - Adds two integers and returns
 * the result.
 * @a: number one.
 * @b: number two.
 * Return: The sum of two input numbers.
 */
int add(int a, int b)
{
	int s;

	s = a + b;
	return (s);
}
